name: shift_alarm_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.0.0+1
  syncfusion_flutter_pdf: ^24.2.9
  flutter_local_notifications: ^17.1.2
  shared_preferences: ^2.2.2
  intl: ^0.18.1
  timezone: ^0.9.2
  path_provider: ^2.1.2 # ADDED FOR FILE SAVING

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
